/* ===========================================================
    VERSION MANAGER - OmniCard

    Gestiona el versionado de archivos estáticos para evitar
    problemas de caché con el navegador
=========================================================== */

const APP_VERSION = '1.0.0'; // Incrementar cada vez que se hagan cambios

export default APP_VERSION;